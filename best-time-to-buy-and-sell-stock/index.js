// input: array of integers (>= 0)
// output: max profit you can get from buying and selling

// strategy:
// find the min, slice starting from min
// find the max of the sliced array
// return max - min

const prices = [2, 7, 3, 5, 1, 6, 3]

function findMaxProfit(array) {
    // loop through prices
    // sell on the highest day
    // buy on the cheapest day before that 
}

findMaxProfit(prices)